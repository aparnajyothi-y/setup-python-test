name: Composite Action Example

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: aparnajyothi-y/setup-python@Feature-request#476
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: requirements/required.txt

      # - name: Install dependencies
      #   run: pip install -r requirements/required.txt

      # - name: Print github.action_path
      #   run: |
      #     echo "The value of github.action_path is: ${{ github.action_path }}"
